export default function SpaModel(
  id,
  title,
  price,
  imgsOrder,
  description
  // createdAt,
) {
  return {
    id,
    title,
    price,
    imgsOrder,
    description,
    // createdAt,
  };
}
